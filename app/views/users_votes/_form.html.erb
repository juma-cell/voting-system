<%= form_with(model: users_vote) do |form| %>
  <% if users_vote.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(users_vote.errors.count, "error") %> prohibited this users_vote from being saved:</h2>

      <ul>
        <% users_vote.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :id, style: "display: block" %>
    <%= form.number_field :id %>
  </div>

  <div>
    <%= form.label :userName, style: "display: block" %>
    <%= form.text_field :userName %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
